name: first Workflow

# To get triggered on every push in this repo in any branch
on: push

jobs:
 job1 : 
   name: job1
   runs-on: ubuntu-latest
   steps: 
     - name: Print Node Version
       run: node --version
      
     - name: Print npm version
       run: npm --version

     - name: Print message
       run: echo "My first GitHub Actions CI Pipeline"  
